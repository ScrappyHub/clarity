# Clarity LAW.md (LOCKED) - Standalone v1 + Packet Constitution v1 Option A

Clarity MUST boot and run standalone. NFL is an integration target, never a dependency.

Packet Constitution v1 (Option A): manifest.json MUST NOT contain packet_id; packet_id.txt does; sha256sums.txt last.
